# chat/urls.py
# from django.urls import path
# from .views import ChatRoomListCreateView, LeaveChatRoomView, EnterChatRoomView, SendMessageView, ListMessagesView

# urlpatterns = [
#     path('chatroom/', ChatRoomListCreateView.as_view(), name='chatroom-list-create'),
#     path('leave/<str:room_id>/', LeaveChatRoomView.as_view(), name='leave-chatroom'),
#     path('enter/<str:room_id>/', EnterChatRoomView.as_view(), name='enter-chatroom'),
#     path('send/<str:room_id>/', SendMessageView.as_view(), name='send-message'),
#     path('list/<str:room_id>/', ListMessagesView.as_view(), name='list-messages'),
# ]
